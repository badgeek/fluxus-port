# build output
build/
spikes/build/
spikes/**/build/
cmake-build*/
_deps/

# spike binaries
spikes/racket/racket_spike
spikes/racket/racket_bind

# macOS / bundles
.DS_Store
*.app/

# user-provided texture assets (not checked in)
assets/

# Racket precompiled bytecode (regenerate with: make precompile)
racket-lib/compiled/
**/compiled/*.zo

# Go control binaries (build with: go build -o <name> .)
mcp/fluxus-mcp
cli/fluxus
